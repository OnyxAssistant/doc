# Introduction

A neuron is a microservice that is responsible for a single task. It will be deployed as a Docker container.

You can then install neurons as plugins in the Onyx core service using the dashboard.